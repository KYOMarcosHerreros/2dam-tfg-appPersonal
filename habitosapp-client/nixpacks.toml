[phases.setup]
nixPkgs = ['nodejs_22', 'npm-10_x']

[phases.install]
cmds = ['npm ci --platform=linux --arch=x64']

[phases.build]
cmds = ['npm run build']

[phases.start]
cmd = 'npm run preview'

[variables]
NODE_VERSION = '22'